# ── VoiceTally Intelligence Service (app/, port 8001) ────────

# App settings
APP_NAME=VoiceTally Intelligence Service
APP_VERSION=1.0.0
DEBUG=True

# STT Provider: "whisper" (local, default) or "openai"
STT_PROVIDER=whisper

# OpenAI API key — only needed if using LLM-based intent classification
# OPENAI_API_KEY=sk-...   # currently no LLM is configured, if intent classification returns none it falls back to LLM but it returns LLM not configured. cause we arent use it yet. (to be cleared again)
